{
  "name": "bouncing_ball",
  "version": "1.0.0",
  "description": "",
  "main": "src/bounce.js",
  "scripts": {
    "test": "jest",
    "build": "npx esbuild bounce.ts --bundle --minify --sourcemap=inline --outfile=./build/index.js",
    "coverage": "npx jest --coverage"
  },
  "author": "IWC",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "esbuild": "^0.16.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.4"
  }
}
